name: Context
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      BRANCH: main
    steps:
      - run: echo "${BRANCH}"
      - run: echo "${{ github.actor }}"
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.BRANCH }}
